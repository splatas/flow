{
  "name": "base-jaxx",
  "version": "0.1.0",
  "main": "server.js",
  "x-git-version": "{{X-GIT-VERSION}}",
  "x-prefix": "base",
  "x-port": 8100,
  "scripts": {
    "start": "node server",
    "openapi": "node stat/openapi.js > stat/openapi.json",
    "pm2": "node stat/pm2.js > stat/pm2.json",
    "fixtures": "node stat/fixtures.js > stat/fixtures.json",
    "test": "npm run pm2 && npm run openapi && npm run fixtures && tap test",
    "coverage": "tap --cov test"
  },
  "dependencies": {
    "fastify": "^2.0.0-rc.4",
    "got": "^9.6.0",
    "jsonwebtoken": "^8.4.0"
  },
  "devDependencies": {
    "fastify-oas": "^2.0.0-rc.4",
    "shipit-deploy": "^4.1.3",
    "tap": "^12.5.1"
  }
}
